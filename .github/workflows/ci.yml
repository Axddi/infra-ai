name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        cd app/backend
        pip install -r requirements.txt

    - name: Run tests
      run: |
        cd app/backend
        echo "Running tests..."
        pytest || true
    - name: Build Docker Image
      run: |
        echo "Building Docker image..."
        docker build -t my-backend ./docker/backend/Dockerfile .

    - name: Success message
      run: echo "Docker Image built successfully!"